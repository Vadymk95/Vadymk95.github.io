const age = prompt("Укажите возраст:");
if (age < 18) {
     alert("Иди рюкзак собирай");
} else if (age < 30) {
     alert("Здарова, епта!))");
} else  {
     alert("Доброго времени суток, сэр!");
}